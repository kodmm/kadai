<h3>Editing Book</h3>
<%= form_for(@book) do |f| %>
	<h4>Title</h4>
	<%= f.text_field :title %>
	<h4>Body</h4>
	<%= f.text_area :body %>

	<%= f.submit '保存' %>

	<%= link_to 'show',book_path(@book) %>
	<%= link_to 'back',books_path %>
<% end %>


